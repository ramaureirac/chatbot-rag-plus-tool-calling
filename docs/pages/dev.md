# Run In Dev Mode

For testing and develop this project you'll need Docker.  
Frist create your own Non-Persistant Milvus instance by running:

    make docker-milvus

Then run the app

    make run
    make serve
    make embed